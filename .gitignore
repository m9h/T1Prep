# custom
.claude
.DS_Store
env
ideas.txt
todo.txt
log*
*.nii
# Container build artefacts: large binaries / datasets copied into the
# docker build context at build time. Not tracked — the neurodebian agent
# builds the .debs out-of-tree and we extract on demand.
comparison/containers/temp_head.nii.gz
comparison/containers/*.deb
internal
webui_uploads
__pycache__
*.webloc
*.m
*.zip
